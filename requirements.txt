# 🛡️ RDDS — Rogue Device Detection System
# Project Dependencies

# ── Core Networking ──────────────────────────
scapy==2.5.0                  # Packet crafting, ARP scan, passive sniff

# ── Web Dashboard ────────────────────────────
flask==3.0.3                  # Dashboard backend
flask-cors==4.0.1             # CORS support for API

# ── Utilities ────────────────────────────────
requests==2.31.0              # HTTP requests (optional features)
python-dotenv==1.0.0          # .env configuration
python-docx==1.1.0            # Security report generation (DOCX format)

# ── AI & Machine Learning ────────────────────
tensorflow==2.15.0            # Deep Learning framework
pandas==2.1.4                 # Data processing
numpy==1.26.4                 # Numeric matrices
scikit-learn==1.3.2           # AI Scaling and metrics
joblib==1.3.2                 # Loading PKL models
